---
"@devopness/ui-react": minor
---

Add `ResourceCardGrid`, a composed component combining `CardGrid`, `ResourceCard` and `Pagination` with an always-first "create resource" tile — the layout used by resource-listing pages (projects, environments, organizations) instead of assembling those primitives by hand.
