---
'@devopness/sdk-js': patch
'@devopness/sdk-python': patch
---

Expose the new server instance spec fields, `memory` and `vcpus`, in the generated SDK models so clients can inspect the selected machine shape when provisioning servers.
