---
"@devopness/sdk-python": minor
---

Adds `strict_validation_mode` configuration option to allow graceful handling of response validation errors. When disabled, returns opaque field-accessible data instead of raising exceptions on validation failures.
