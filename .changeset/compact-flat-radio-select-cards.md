---
'@devopness/ui-react': minor
---

Add a compact flat `RadioSelectCards` presentation that renders the gallery-style cards without requiring grouped sections, and document it with a new Storybook variant.
