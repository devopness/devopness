---
'@devopness/ui-react': minor
---

Add a compact flat `RadioSelectCards` presentation that can hide the visible radio indicator without requiring grouped cards, and document it with a new Storybook variant.
