---
"@devopness/ui-react": patch
"@devopness/sdk-js": patch
---

Add package-local `lint:fix` scripts and clarify that linting should be run from each package directory so package-specific configuration is used.
