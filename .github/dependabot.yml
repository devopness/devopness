version: 2
updates:
- package-ecosystem: npm
  directory: "/"
  schedule:
    interval: daily
  open-pull-requests-limit: 10
  groups:
    # Storybook first: eslint-plugin-storybook also matches eslint-* in linters.
    storybook:
      patterns:
        - "storybook"
        - "@storybook/*"
        - "eslint-plugin-storybook"
    linters:
      patterns:
        - "typescript-eslint"
        - "@typescript-eslint/*"
        - "eslint"
        - "@eslint/*"
        - "eslint-*"
        - "prettier"
        - "prettier-*"
        - "@ianvs/prettier-plugin-sort-imports"
        - "typescript"
    jest:
      patterns:
        - "jest"
        - "ts-jest"
        - "@types/jest"
    vite:
      patterns:
        - "vite"
        - "@vitejs/*"
        - "vite-*"
        - "vitest"
        - "@vitest/*"

- package-ecosystem: pip
  directories:
    - "/packages/sdks/python"
  schedule:
    interval: daily
  open-pull-requests-limit: 10

- package-ecosystem: npm
  directory: "/docs"
  schedule:
    interval: daily
  open-pull-requests-limit: 10
  groups:
    fumadocs:
      patterns:
        - "fumadocs-*"
    linters:
      patterns:
        - "typescript-eslint"
        - "@typescript-eslint/*"
        - "eslint"
        - "@eslint/*"
        - "eslint-*"
        - "@next/eslint-plugin-next"
        - "prettier"
        - "prettier-*"
        - "@ianvs/prettier-plugin-sort-imports"
        - "typescript"
        - "@types/node"
        - "@types/react"
        - "@types/react-dom"
        - "@types/mdx"
    tailwind:
      patterns:
        - "tailwindcss"
        - "@tailwindcss/*"
    next:
      patterns:
        - "next"
