name: "CLA Assistant"

on:
  issue_comment:
    types:
      - created
  pull_request_target:
    types:
      - opened
      - closed
      - synchronize

jobs:
  cla_assistant:
    name: CLA Assistant
    uses: devopness/clabot-config/.github/workflows/cla.yml@main
    secrets: inherit
