import { styled } from 'styled-components'

const SourceAndHashSpan = styled.span`
  margin-right: 0px;
  user-select: none;
`

const WrapperCommit = styled.div`
  display: flex;
`
export { SourceAndHashSpan, WrapperCommit }
