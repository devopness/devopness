"""
Devopness API Python SDK - Painless essential DevOps to everyone

Note:
    This is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
"""

from .. import DevopnessBaseService, DevopnessBaseServiceAsync, DevopnessResponse
from ..models import SearchResourceRelation
from ..utils import parse_query_string


class SearchApiService(DevopnessBaseService):
    """
    SearchApiService - Auto Generated
    """

    def list_search_resources(
        self,
        organization_id: str,
        q: str,
        resource_type: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> DevopnessResponse[list[SearchResourceRelation]]:
        """
        Search resources the user can access in Devopness.

        Attributes:
            organization_id (str): The organization ID or URL Slug to search within.
            q (str): The search expression used to match resources. Must be at least 2 characters. Must not be greater than 255 characters.
            resource_type (str): The resource family to search.
            page (Optional[int]): Number of the page to be retrieved
            per_page (Optional[int]): Number of items returned per page

        Raises:
            DevopnessApiError: If an API request error occurs.
            DevopnessNetworkError: If a network error occurs.
        """

        query_string = parse_query_string(
            {
                "page": page,
                "per_page": per_page,
                "organization_id": organization_id,
                "q": q,
                "resource_type": resource_type,
            }
        )

        endpoint_parts = [
            "/search",
            f"?{query_string}" if query_string else "",
        ]

        endpoint: str = "".join(endpoint_parts)
        response = self._get(endpoint)

        return DevopnessResponse(response, list[SearchResourceRelation])


class SearchApiServiceAsync(DevopnessBaseServiceAsync):
    """
    SearchApiServiceAsync - Auto Generated
    """

    async def list_search_resources(
        self,
        organization_id: str,
        q: str,
        resource_type: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> DevopnessResponse[list[SearchResourceRelation]]:
        """
        Search resources the user can access in Devopness.

        Attributes:
            organization_id (str): The organization ID or URL Slug to search within.
            q (str): The search expression used to match resources. Must be at least 2 characters. Must not be greater than 255 characters.
            resource_type (str): The resource family to search.
            page (Optional[int]): Number of the page to be retrieved
            per_page (Optional[int]): Number of items returned per page

        Raises:
            DevopnessApiError: If an API request error occurs.
            DevopnessNetworkError: If a network error occurs.
        """

        query_string = parse_query_string(
            {
                "page": page,
                "per_page": per_page,
                "organization_id": organization_id,
                "q": q,
                "resource_type": resource_type,
            }
        )

        endpoint_parts = [
            "/search",
            f"?{query_string}" if query_string else "",
        ]

        endpoint: str = "".join(endpoint_parts)
        response = await self._get(endpoint)

        return await DevopnessResponse.from_async(
            response, list[SearchResourceRelation]
        )
