        {{#hasQueryParams}}

        query_string = parse_query_string({ {{#queryParams}}"{{baseName}}": {{paramName}},{{/queryParams}} })
        {{/hasQueryParams}}

        endpoint_parts = [
            {{#hasPathParams}}f{{/hasPathParams}}"{{{path}}}",
            {{#hasQueryParams}}f"?{query_string}" if query_string else "",{{/hasQueryParams}}
        ]

        endpoint: str = "".join(endpoint_parts)
