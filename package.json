{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "devopness",
  "workspaces": [
    "packages/sdks/*",
    "packages/ui/*"
  ],
  "scripts": {
    "build:all": "npm run build:release --workspaces",
    "release": "npm run build:all && npx changeset publish"
  }
}

