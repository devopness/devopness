# This file is auto generated. DO NOT edit this file manually!
type: object
properties:
  id:
    description: 'The unique ID of the given Variable.'
    example: 1
    type: integer
  key:
    description: "The unique key used to identify the variable on the target.\n                    For `resource-config-file`, use a relative path such as `.env` or `storage/oauth-public.key`.\n                    For `resource-symlink`, `key` is the link path and `value` is the destination path, for example `node_modules` -> `${APP_SHARED_DIR}/node_modules`. Must not be greater than 100 characters."
    example: APP_URL
    type: string
  value:
    description: "The value to be assigned to this variable when deployed to its target.\n                    When variable is of type `file`, this is the file content.\n                    When variable is of type `symlink`, this is the destination path, for example `${APP_SHARED_DIR}/node_modules`. Must not be greater than 21504 characters."
    example: 'https://staging.devopness.com'
    type: string
  description:
    description: 'A text describing the variable, provided by the end user. Must not be greater than 255 characters.'
    example: 'Sample variable description'
    type: string
  target:
    $ref: '#/components/schemas/VariableTarget'
  type:
    $ref: '#/components/schemas/VariableType'
  hidden:
    description: 'Indicates if the variable value should be visible or not in the deployment logs.'
    example: false
    type: boolean
required:
  - id
  - key
  - value
  - target
  - type
  - hidden
