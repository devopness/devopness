# This file is auto generated. DO NOT edit this file manually!
type: object
description: 'Authentication method settings for source providers'
properties:
  auth_type:
    type: string
    description: 'The authentication type identifier'
    example: oauth
  auth_type_human_readable:
    type: string
    description: 'Human-readable name for the authentication method'
    example: 'GitHub OAuth'
  hint:
    type: string
    description: 'Hint text explaining the authentication flow'
    example: 'Authorize Devopness to access your repositories using OAuth.'
  recommended:
    type: boolean
    description: 'Whether this authentication method is recommended for this provider'
    example: true
  connect_url:
    type: string
    description: 'The URL to redirect the user for authentication. For OAuth methods, this is the authorization URL. For application installation methods (e.g., GitHub App), this is the installation URL.'
    example: 'https://github.com/login/oauth/authorize?client_id=xxx&redirect_uri=https://app.devopness.com/callback&scope=repo,read:org'
  input_settings:
    $ref: '#/components/schemas/ProviderInputSettingsList'
  provider_config:
    $ref: '#/components/schemas/ProviderCallbackConfigList'
required:
  - auth_type
  - input_settings
