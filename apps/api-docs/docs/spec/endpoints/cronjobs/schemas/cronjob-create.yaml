type: object
properties:
  command:
    type: string
    description: The command line to be executed when running the task
    example: "echo 'devopness rocks!' | tee -a /tmp/task-output.log"
  pattern:
    type: string
    description: A cron expression consisting of Minute, Hour, Day of Month, Month and Day of Week subexpressions
    example: "0 0 * * *"
  run_as_user:
    type: string
    description: The name of the Unix user on behalf of which the task will be executed
    example: root
  auto_generated:
    default: false
    type: boolean
    description: Indicates if the task was auto_generated by `devopness` itself
required:
  - command
  - pattern
  - run_as_user
