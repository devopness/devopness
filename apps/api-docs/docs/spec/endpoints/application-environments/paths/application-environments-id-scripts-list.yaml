summary: List scripts to be executed as custom deployment steps of an application environment
operationId: listApplicationEnvironmentScripts
tags:
  - Applications - Environments
parameters:
  - $ref: '#/components/parameters/acceptHeader'
  - $ref: '#/components/parameters/authHeader'
  - $ref: '#/components/parameters/contentTypeHeader'
  - in: path
    name: application_environment_id
    example: 1
    schema:
      type: integer
      minimum: 1
    required: true
    description: Unique ID of the application environment to retrieve scripts from
responses:
  200:
    description: 'Ok'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ApplicationEnvironmentScripts'
