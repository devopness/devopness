type: object
properties:
  name:
    $ref: '#/components/schemas/ServerCloudServiceName'
  settings:
    anyOf:
      - $ref: '#/components/schemas/CloudAwsEc2Settings'
      - $ref: '#/components/schemas/CloudDigitalOceanDropletSettings'
    description: The configurations used to cloud service
