type: object
properties:
  message:
    type: string
    format: string
    description: Output message indicating success or failure
    example: "Server successfully connected"
required:
  - message
