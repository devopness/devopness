type: object
properties:
  email:
    type: string
    format: email
    description: The user's registered e-mail address
  password:
    type: string
    format: password
required:
  - email
  - password
