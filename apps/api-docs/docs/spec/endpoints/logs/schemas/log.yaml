type: object
properties:
  output:
    deprecated: true
    type: string
    description: Please use `servers[i].steps[j].output` instead of this deprecated field that will soon be removed
    nullable: true
  servers:
    $ref: '#/components/schemas/ServerActionLogs'
required:
  - output
  - servers
