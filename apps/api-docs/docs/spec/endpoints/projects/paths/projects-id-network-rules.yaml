summary: Add a network tule to the given project
tags:
  - Projects - Network Rules
parameters:
  - $ref: '#/components/parameters/authHeader'
  - $ref: '#/components/parameters/acceptHeader'
  - $ref: '#/components/parameters/contentTypeHeader'
  - in: path
    name: id
    example: 1
    schema:
      type: integer
      minimum: 1
    required: true
    description: The project numeric Id
requestBody:
  description: A JSON object containing backgroud process data
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/NetworkRuleCreate'
responses:
  201:
    description: 'Created'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/NetworkRule'
  422:
    $ref: '#/components/responses/ValidationError'
