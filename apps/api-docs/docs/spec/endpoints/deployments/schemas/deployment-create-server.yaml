type: object
properties:
  id:
    type: integer
    format: integer
    description: Numeric ID of the server
    example: 123
  hostname:
    type: string
    description: The server's hostname
    example: web-srv-1
required:
  - id
  - hostname
