type: string
description: The action to be executed for the given service
enum:
  [
    status,
    start,
    stop,
    reload,
    restart,
    enable,
    disable,
    install,
    uninstall,
  ]
example: "active"
