type: string
description: The application deployment event to which it's attached
enum:  [check-app-environment, get-new-release, install-dependencies, activate-new-release]
example: 'install-dependencies'
